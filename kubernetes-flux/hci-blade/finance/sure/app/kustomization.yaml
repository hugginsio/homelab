# yaml-language-server: $schema=https://json.schemastore.org/kustomization

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - "./externalsecret.yaml"
  - "./helmrepository.yaml"
  - "./helmrelease.yaml"
  - "./httproute.yaml"
generatorOptions:
  disableNameSuffixHash: true
configMapGenerator:
  - name: sure-chart-values
    files:
      - values.yaml
