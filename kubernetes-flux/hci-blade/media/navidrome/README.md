# navidrome

## Mobile App Onboarding

1. `exec` into the Navidrome pod.
2. Run `navidrome user list` and determine the username of the user you wish to onboard.
3. Run `navidrome user edit -u <username> --set-password` and set the password accordingly.

This password can be used for mobile access where SSO is not supported.
